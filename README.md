# Biomedical Data Pipeline

A Python tool for cleaning and exploring structured biomedical CSV data.

It checks missing values, valid ranges, and duplicate rows, then saves a cleaned CSV, a change log, and an EDA report with charts.

## How to run it? 
```bash
pip install -r requirements.txt
```

